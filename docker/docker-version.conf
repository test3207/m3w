# Docker base image configuration
# Single source of truth for build scripts (build-docker.ps1, docker-build.ps1)
#
# NOTE: Dockerfile FROM statements are hardcoded and must be updated manually:
#   - docker/Dockerfile
#   - docker/Dockerfile.backend
#   - docker/Dockerfile.frontend

# Node.js base image for building and runtime
NODE_IMAGE=node:25.2.1-alpine
