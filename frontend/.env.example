# Vite Frontend Environment Variables
# All variables must be prefixed with VITE_ to be exposed to the browser

# Backend API URL (development)
VITE_API_URL=http://localhost:4000

# Backend API URL (production)
# VITE_API_URL=https://api.example.com

# Demo mode: controls demo feature detection API calls
# - true:  Enable demo mode detection (calls /api/demo/storage)
# - false: Disable demo mode (no API calls, no demo banner)
# - unset: Follows BUILD_TARGET (rc=enabled, prod=disabled)
VITE_DEMO_MODE=false

# Remote logging: enable sending logs to backend (development only)
# NOTE: In production, this is controlled by runtime injection via window.__ENABLE_REMOTE_LOGGING__
#       This env var only works in local development for testing purposes.
# - true:  Enable remote logging (logs sent to backend /api/logs)
# - false: Console only (default)
# VITE_ENABLE_REMOTE_LOGGING=false
