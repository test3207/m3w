/**
 * Player components exports
 * 
 * Note: PlayQueueDrawer is intentionally NOT exported here.
 * It's lazily loaded in mobile-layout.tsx for code splitting.
 */

export { MiniPlayer } from "./mini-player";
export { FullPlayer } from "./full-player";
